# Files to add next

1. Copy the full Android Studio project into `android/EmergencyMobile/`.
2. Keep `android/ApiConstants.example.kt` public-safe.
3. Put Lambda source code under `backend/lambdas/`.
4. Put CloudFormation templates under `infrastructure/cloudformation/`.
5. Add report sources under `report/`.
6. Add slides under `slides/`.
